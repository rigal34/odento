{% extends 'base.html.twig' %}

{% block title %}Hello PostController!{% endblock %}

{% block body %}
    {% for post in posts %}
        <h1>{{ post.title }}</h1>
        <p>{{ post.content|raw }}</p>
        <a href="{{ path('app_actu_detail', {'id': post.id}) }}" class=>Déail du post</a>
        <hr>
    {%endfor %}
{% endblock %}
