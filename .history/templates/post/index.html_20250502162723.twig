{% extends 'base.html.twig' %}

{% block title %}Hello PostController!{% endblock %}

{% block body %}
    {% for post in posts %}
        <h1>{{ post.title }}</h1>
        <p>{{ post.content|raw }}</p>
        <a>
        <hr>
    {%endfor %}
{% endblock %}
